diff --git a/node_modules/@graphprotocol/graph-cli/dist/commands/test.js b/node_modules/@graphprotocol/graph-cli/dist/commands/test.js
index 4c94661..a72d034 100644
--- a/node_modules/@graphprotocol/graph-cli/dist/commands/test.js
+++ b/node_modules/@graphprotocol/graph-cli/dist/commands/test.js
@@ -146,7 +146,7 @@ async function getPlatform(matchstickVersion, logsOpt) {
     const type = os_1.default.type();
     const arch = os_1.default.arch();
     const cpuCore = os_1.default.cpus()[0];
-    const isAppleSilicon = arch === 'arm64' && /Apple (M1|M2|M3|processor)/.test(cpuCore.model);
+    const isAppleSilicon = arch === 'arm64' && /Apple (M\d+|processor)/.test(cpuCore.model);
     const linuxInfo = type === 'Linux' ? await getLinuxInfo.bind(this)() : {};
     const linuxDistro = linuxInfo.name;
     const release = linuxInfo.version || os_1.default.release();
